[Unit]
Description=WISE site + on-page comment API (port 4144)
After=network.target

[Service]
Type=simple
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/ubuntu/wisealliance/server
# The site itself is served from the git checkout, so a `git pull` is still the
# whole deploy. Comments live outside the checkout so pulling never touches them.
Environment=WISE_FEEDBACK_STATIC=/home/ubuntu/wisealliance
Environment=WISE_FEEDBACK_DB=/home/ubuntu/wise-feedback/comments.db
# A local checkout writes to this same store, so notes left while working
# locally and notes left by reviewers on the server are one shared thread.
Environment=WISE_FEEDBACK_ALLOW_LOCALHOST=1
# How the owner is signed on their own notes and replies. Anyone holding the
# admin key posts under this name and is badged, so an answer is never
# mistaken for a note from the person who raised the thread.
Environment=WISE_FEEDBACK_OWNER=AeyKay
EnvironmentFile=/home/ubuntu/wise-feedback/secret.env
ExecStart=/home/ubuntu/wise-feedback/venv/bin/gunicorn \
    --workers 2 \
    --threads 4 \
    --bind 0.0.0.0:4144 \
    feedback_api:app
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
